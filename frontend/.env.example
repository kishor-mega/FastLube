# Production / deployed frontend — set to your live backend URL (no trailing slash)
# REACT_APP_API_URL=https://your-backend.onrender.com

# Local dev: leave unset — npm start uses proxy to http://localhost:5000
