# Optional: full backend URL when not using the dev proxy (e.g. production build or mobile testing).
# Leave empty for local dev with `npm start` — package.json proxy forwards /api to the server.
# REACT_APP_API_URL=http://localhost:5000
